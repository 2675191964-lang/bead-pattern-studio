# 当前交付验证记录

验证日期：2026-08-30（Asia/Shanghai）。

## 自动化门禁

- `pnpm lint`：通过，0 warning。
- `pnpm typecheck`：通过。
- `pnpm test:run`：6 个测试文件、14 项测试全部通过；其中包含 20 组确定性合成边界样本、大画布像素预算和低频关键色保留回归。
- `pnpm build`：通过；PWA 生成 12 个预缓存条目，约 1.08 MiB。
- `pnpm test:e2e`：真实 Chrome 桌面与 360×800 移动视口共 4 条流程全部通过。
- `pnpm audit --prod --audit-level high`：无已知漏洞。
- `pnpm licenses list --prod --json`：生产依赖许可证已复核并写入 `THIRD_PARTY_NOTICES.md`。

## 真实浏览器与导出检查

- 100×100 纯红 PNG 生成 29×29 网格：841 颗、1 个色号。
- 编辑器中心格擦除后为 840 颗，撤销回到 841；等待自动保存并刷新后恢复 841。
- 导出页守恒：网格 841 = 分色合计 841 = CSV 必需量 841。
- 实际下载 CSV，必需量 841、8% 损耗后建议量 909、1000 颗/包需 1 包。
- 实际下载两页 A4 PDF，并用 Poppler 渲染逐页目视检查：材料表、29×29 分板图、行列坐标、色号和 50 mm 校准线均可读且无裁切。
- 360×800 页面 `scrollWidth = clientWidth = 360`，上传、生成、编辑器、导出页均无横向溢出。
- Service Worker 安装并接管后，断网打开 `/help` 深链成功。
- E2E 监听上传、生成和导出主流程，未发现任何非 localhost 请求。
- GitHub Pages 子路径构建使用哈希路由，避免静态托管刷新动态编辑/导出地址时返回 404。
- 用户提供的 1280×1280 JPEG 已在隔离 Chrome 中按 200×200、18 色真实复测：转换约 516 ms，40,000 格完整显示；预览画布由旧版 2800×2800 自动收敛至 477×477，编辑区无横纵溢出，刷新后项目恢复正常。
- 同一图片旧算法选出的 18 色均为浅色，粉色眼睛和深色轮廓丢失；新版保留 E4、F7、P23 等低频粉/深红色，人物眼睛和轮廓重新可辨。

## 证据边界

以上证明本机实现、自动化检查和真实浏览器验收通过，不等同于品牌官方色卡认证或实体批次校色。任务书要求的 20 张真实人物/宠物/风景/卡通/Logo 人工质量评审尚未执行，因为附件中没有提供可合法复测的图片集；当前仅以 20 组合成边界样本覆盖算法回归。
