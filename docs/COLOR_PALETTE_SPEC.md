# 色卡格式

每个颜色包含稳定 `brandId/paletteId/code`、中英文名称、`#RRGGBB`、统一计算的 Lab、系列、启用状态、来源 URL 与来源日期。导入时从 CSV 的 RGB 重新按 sRGB/D65 计算 Lab，不信任手工 Lab。

项目保存完整 `paletteSnapshot`。以后色卡更新不会改变旧项目的颜色索引和统计。

内置 RGB/Hex 仅代表社区维护的屏幕近似值。真实豆子会受批次、光源、相机和显示器影响；用于生产或售卖前应在固定 D65 光源和校色流程下建立自有批次测量色卡。
